services:
  mantle:
    image: ghcr.io/open-webui/open-webui:latest
    container_name: mantle
    restart: unless-stopped
    ports:
      - "3001:8080"
    volumes:
      - /root/frontend/open-webui:/app/backend/data
    networks:
      homelab_network:
        aliases:
          - open-webui
    environment:
      - WEBUI_NAME=MANTLE
      - WEBUI_SECRET_KEY=abcdgdgsouwqiouehfnjshauit1y2ohjyouyoji21
      - WEBUI_URL=https://chat.my-mantle.com
      - DATABASE_URL=postgresql://postgres:Limj3579@db:5432/openwebui
      - ENABLE_SIGNUP=true
      - DEFAULT_USER_ROLE=user

networks:
  homelab_network:
    external: true
